import { LocalStorageProducts } from './local-storage-products.model';

describe('LocalStorageProducts', () => {
  it('should create an instance', () => {
    expect(new LocalStorageProducts()).toBeTruthy();
  });
});
